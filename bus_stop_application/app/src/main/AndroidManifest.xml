<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thinking.juicer.busstopapplication">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusStopApplication"
        android:usesCleartextTraffic="true"><activity android:name=".CheckNotificationActivity" android:screenOrientation="unspecified" android:theme="@style/popupTheme" />
        <activity android:name=".GetOffNotificationActivity" android:screenOrientation="unspecified" android:theme="@style/popupTheme" />
        <activity android:name=".SelectedRouteInfo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>